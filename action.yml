name: Create build artifact

inputs:
  linode-pat:
    required: true

  linode-label:
    required: true

  linode-root-pass:
    required: false

  linode-admin-users-file:
    required: false

  domains:
    required: true

  email:
    required: true

  deploy-artifact:
    required: true

  deploy-command:
    required: true

  deploy-directory:
    required: false

  deploy-user:
    required: false
    default: deploy

  deploy-user-private-key:
    required: true

  deploy-user-public-key:
    required: true

runs:
  using: node12
  main: dist/index.js


runs:
  using: node12
  main: dist/index.js
